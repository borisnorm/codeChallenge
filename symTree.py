Check if a tree is symetric

  function symCheck(root):
    leftTree =
    rightTree =

  leftDFS(node, array):
    leftDFS(node.left. array)
    array.append(node)
    leftDFS(node.right), array

  rightDFS(node, array):
    rightDFS(node.right, array)
    array.append(node)
    leftDFS(node.left, array)










