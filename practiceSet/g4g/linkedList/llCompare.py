#Compare if two strings of ll are equal/palindrome/reversed

#Implement the list abstractions with next and prev
#as functions
def ll_compare(llOne, llTwo):
  headPointer = llOne
  tailPointer = llTwo
  while headPointer != tailPointer:
    if headPointer.value !=  tailPointer.value
      return False
    headPointer = headPointer.next
    tailPointer = tailPointer.prev
  return True

