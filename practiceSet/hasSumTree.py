#Verify is a given tree contains a subtree,

#Search for all the same roots of the tree,
#For each root, execute a DFS, they should be identical, simul dfs

def has_subtree(main_tree, sub_tree):
  trackerOne = null
  trackerTwo = null
  return DFS_Tree() || DFS_Tree()

#BST or Non BST, the harder case
def DFS_Tree(root, trackerOne, trackerTwo):
  position = root
  DFS_Tree(root.left, position)
  DFS_Tree(root.right, position)











